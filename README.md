some learning notes,u guys can read just by diffrent branches; in case u didn't see anything that means i get no time to update it.
includes -> {
  adb,
  c,
  c++,
  java,
  JNI,
  kotlin,
  linux,
  python,
  汇编指令
} 
